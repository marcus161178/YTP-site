<%= form_for(@client_video) do |f| %>
  <% if @client_video.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@client_video.errors.count, "error") %> prohibited this client_video from being saved:</h2>

      <ul>
      <% @client_video.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.hidden_field :user_id, :value => @user_id %><br />
</div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
