<%= form_for(@vendor_image) do |f| %>
  <% if @vendor_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vendor_image.errors.count, "error") %> prohibited this vendor_image from being saved:</h2>

      <ul>
      <% @vendor_image.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label "Subcategory" %><br />
    <%= f.collection_select "subcategory_id", Subcategory.all, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :portfolio %><br />
    <%= f.file_field :portfolio %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
